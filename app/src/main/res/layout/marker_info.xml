<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:background="@drawable/layout_with_5dp_corner"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/name.text"
        android:textColor="@color/black"
        android:textAppearance="@android:style/TextAppearance.Material.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_gravity="start"
        android:layout_toStartOf="@id/home.button"
        tools:text="Name" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/home.button"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:background="@null"
        android:scaleType="fitXY"
        android:tint="@color/darkGray"
        android:src="@drawable/icon_home" />

    <TextView
        android:id="@+id/capital.text"
        android:textColor="@color/darkGray"
        android:textAppearance="@android:style/TextAppearance.Material.Body1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/name.text"
        android:layout_gravity="end"
        tools:text="Capital" />
    <TextView
        android:id="@+id/distance.text"
        android:visibility="gone"
        android:textColor="@color/lightGray"
        android:textAppearance="@android:style/TextAppearance.Material.Body2"
        android:layout_below="@id/capital.text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        tools:text="distance"/>

</RelativeLayout>